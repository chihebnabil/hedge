# Benchmark artifacts

`results.json` and `RESULTS.md` in this folder hold the WikiText-2 benchmark
numbers reproduced by `../run_benchmark.py` (staged, see --help). Model
pickles are intentionally not committed — regenerate with the stages above.
